# Auto-decisions — cmux clipboard-over-SSH study (--auto)

- 12:14 Route-card confirm skipped (--auto). Route: R19 thinned — xia recon → compare → conditional adapt into existing fix worktree → agent self-test. Why: mechanism study feeding an in-flight fix, not a fresh port; full predict/plan gates would duplicate the reviewed worktree's own trail. Risk: low. Alternatives rejected: full R19 (predict+plan) — redundant; R14 research-only — user explicitly asked to adapt & test. Reversibility: full (worktree uncommitted).
- 12:14 Self-test scoping: TUI key-paste e2e cannot run headlessly (no key-injection CLI; agent-browser n/a for a TUI). Will run test suite + scripted federation checks and STATE the manual paste check plainly instead of claiming e2e ran. Per cortex E2E fallback rule.
- 12:20 Adapt stage adjudicated: NO code adaptation. Why: recon proves cmux uses the same mechanism (temp file → scp → paste path into pty) and herdr's implementation is stricter on every compared axis; nothing to fold in. Alternatives rejected: porting cmux's base64 RPC hop (strictly worse over a native channel); fixed 45s timeout (herdr's payload-proportional one is better). Multi-image space-separated paths noted as optional follow-up only. Reversibility: n/a (no change made).
- 12:20 Self-test scope executed as: focused cargo tests on the fix worktree (image_path + input paste/gate tests) as attestation; TUI key-paste e2e remains a manual gate — explicitly NOT claimed as run.
